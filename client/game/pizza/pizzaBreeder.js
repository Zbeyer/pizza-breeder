// export default function createRandom(generator) {
function pizzaBreeder(pizzaA, PizzaB) {
	let numberOfPizzasToProduce = 0;
	let toppings = [];
	let traits = [];

	// https://www.freecodecamp.org/news/how-to-clone-an-array-in-javascript-1d3183468f6a/
	// if (pizzaA.toppings)
	// {
	// 	Object.keys(pizzaA.toppings)
	// }

	/**
	 * Heres our first little test
	 * 		We need to copy the contents from both pizza's toppings and average the strength of those toppings
	 * 		If a topping exists on both pizzas its (toppingA.strength + toppingB.strength) / 2
	 * 		If a topping exists on only one its averaged but the missing value is zero...
	 */

}
// export const PizzaBreeder = createRandom(generator);
